<!-- This code was generated by a tool: ViewTemplates\EntityEmbeddedSearchResultsViewTemplate.tt -->
<UserControl x:Class="Admin.LocationModule.Views.LocationEmbeddedSearchResultsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:Triggers="clr-namespace:Common.UI.Triggers;assembly=Common" xmlns:ValueConverters="clr-namespace:Common.UI.ValueConverters;assembly=Common" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
        <ValueConverters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
      <NullableBoolConverter x:Key="NullToBoolean" />
  </UserControl.Resources>
        <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="No Search Results Found" Visibility="{Binding Locations.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=true}" />


            <DataGrid x:Name="grid" ItemsSource="{Binding Locations}" Visibility="{Binding Locations.Count, Converter={StaticResource CountToVisibilityConverter}}" VerticalAlignment="Stretch" SelectedItem="{Binding SelectedLocation}" AutoGenerateColumns="False" CellStyle="{DynamicResource GridNoTab}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewKeyDown">
                        <Triggers:CallDataMethod Method="NavigateToDetail" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <Triggers:CallDataMethod Method="NavigateToDetailDoubleClick" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="Sorting">
                        <Triggers:CallDataMethod Method="Sorting" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Type}" Header="Type" IsReadOnly="True"  CellStyle="{DynamicResource GridTab}"  />
                            <DataGridTextColumn Binding="{Binding Name}" Header="Name" IsReadOnly="True"  />
                            <DataGridTextColumn Binding="{Binding ParentName}" Header="Parent" IsReadOnly="True"  />
                    </DataGrid.Columns>
        </DataGrid>
        </Grid>
</UserControl>
    