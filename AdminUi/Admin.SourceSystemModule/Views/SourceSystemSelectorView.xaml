<!-- This code was generated by a tool: ViewTemplates\EntitySelectorViewTemplate.tt -->
<UserControl x:Class="Admin.SourceSystemModule.Views.SourceSystemSelectorView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:Controls="clr-namespace:VIBlend.WPF.Controls;assembly=VIBlend.WPF.Editors" xmlns:Triggers="clr-namespace:Common.UI.Triggers;assembly=Common" xmlns:prism="clr-namespace:Microsoft.Practices.Prism.Regions;assembly=Microsoft.Practices.Prism" xmlns:ValueConverters="clr-namespace:Common.UI.ValueConverters;assembly=Common" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ValueConverters:CountToVisibilityConverter x:Key="CountToVisibility"></ValueConverters:CountToVisibilityConverter>
        <ValueConverters:NullToBooleanConverter x:Key="NullToBoolean" />
    </UserControl.Resources>
    <Grid x:Name="Dialog" VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500" MinHeight="150" MinWidth="250">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="1" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource GenericLineGeometryBrush}" Background="White" />
        <Border Grid.Row="1" Grid.ColumnSpan="1"  VerticalAlignment="Stretch" Height="Auto" Margin="0" CornerRadius="0,0,8,8" BorderThickness="1" BorderBrush="Transparent" Background="#FFE2E2E2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" Margin="5,10,25,10">
                    <Button Content="OK" HorizontalAlignment="Stretch" Width="73.507" Margin="5" Height="25" IsEnabled="{Binding SelectedSourceSystem, Converter={StaticResource NullToBoolean}}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <Triggers:CallDataMethod Method="SelectSourceSystem" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                    <Button Content="Cancel" HorizontalAlignment="Stretch" Width="73.507" Margin="5" Height="25" IsCancel="True">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <Triggers:CallDataMethod Method="CancelSelection" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        <!--<Path Grid.Row="1" Fill="{x:Null}" Stretch="Fill" Stroke="#4C000000" Margin="0.485,0,0.485,0" VerticalAlignment="Top" Height="1" Data="M3,0.61866667 L358.69306,0.61866667" Grid.ColumnSpan="1" />-->
        <!--<Path Grid.Row="1" Fill="{x:Null}" Stretch="UniformToFill" Stroke="Red" Margin="0.485,1,0.485,0" VerticalAlignment="Top" Height="5" Data="M 3,0 L 100,0" Grid.ColumnSpan="1" />-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Frame Width="36" Height="36" Margin="20,25,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="False" Template="{DynamicResource icMagnifier}" />
            <Grid  Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Margin="20,25,50,10" Text="Search for Source System" TextWrapping="Wrap" Foreground="#FF0F3F6E" FontSize="20" />
                <Controls:DateTimePicker x:Name="dateTimePicker" Grid.Row="0" Grid.Column="1" Margin="0,30,30,0" Height="20" VerticalAlignment="Top" FormatValue="dd MMM yyyy HH:mm:ss" Culture="en-GB" SelectedDate="{Binding AsOf, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                <Grid  Grid.Row="1" Grid.ColumnSpan ="3" Margin="20,0,30,0" HorizontalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SearchCriteriaTextBox" Grid.Row="0" Grid.Column="0" Margin="0,10,0,10" HorizontalAlignment="Stretch" Text="{Binding EntitySearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource SearchCriteriaTextBoxStyle}"></TextBox>
                    <Button x:Name="Search" Grid.Row="0 " IsDefault="True" Grid.Column="1" Width="80" Height="23.333" Margin="2,0,0,0" x:FieldModifier="public" Content="_Search" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <Triggers:CallDataMethod Method="SearchForEntity" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                    <ListBox Grid.Row="1" Grid.Column="0" Height="200" Margin="0,0,0,10" ItemsSource="{Binding SourceSystems}" SelectedItem="{Binding SelectedSourceSystem}" DisplayMemberPath="Name">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel Margin="3" VirtualizingStackPanel.IsVirtualizing="True" IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <EventSetter Event="MouseDoubleClick" Handler="SelectSourceSystemMDC" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
