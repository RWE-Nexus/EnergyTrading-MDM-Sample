<!-- This code was generated by a tool: ViewTemplates\EntitySearchResultsViewTemplate.tt -->
<UserControl x:Class="Admin.BrokerModule.Views.BrokerSearchResultsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:Triggers="clr-namespace:Common.UI.Triggers;assembly=Common" xmlns:ValueConverters="clr-namespace:Common.UI.ValueConverters;assembly=Common" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
        <ValueConverters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"></ValueConverters:CountToVisibilityConverter>
        <NullableBoolConverter x:Key="NullToBoolean" />
  </UserControl.Resources>
        <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="No Search Results Found" Visibility="{Binding Brokers.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=true}"></TextBlock>


            <DataGrid x:Name="grid" ItemsSource="{Binding Brokers}" Visibility="{Binding Brokers.Count, Converter={StaticResource CountToVisibilityConverter}}" VerticalAlignment="Stretch" SelectedItem="{Binding SelectedBroker}" AutoGenerateColumns="False" CellStyle="{DynamicResource GridNoTab}" ScrollViewer.CanContentScroll="False"> 
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewKeyDown">
                        <Triggers:CallDataMethod Method="NavigateToDetail" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <Triggers:CallDataMethod Method="NavigateToDetailDoubleClick" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="Sorting">
                        <Triggers:CallDataMethod Method="Sorting" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Name}" Header="Name" IsReadOnly="True"  CellStyle="{DynamicResource GridTab}"  />
                            <DataGridTextColumn Binding="{Binding Fax}" Header="Fax" IsReadOnly="True"  />
                            <DataGridTextColumn Binding="{Binding Phone}" Header="Phone" IsReadOnly="True"  />
                            <DataGridTextColumn Binding="{Binding PartyName}" Header="Party" IsReadOnly="True"  />
                    </DataGrid.Columns>
        </DataGrid>
        </Grid>
</UserControl>
    